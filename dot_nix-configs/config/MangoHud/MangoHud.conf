### MangoHud Complete Config – Logs Everything

# ========== ON-SCREEN DISPLAY ==========
fps
frametime
frame_timing
gpu_stats
gpu_temp
gpu_core_clock
gpu_mem_clock
cpu_stats
cpu_temp
cpu_mhz
ram
swap
position=top-left
font_size=20
text_outline
background_alpha=0.3

# ========== I/O METRICS (logged to CSV) ==========
io_read
io_write

# ========== LOGGING SETTINGS ==========
# autostart_log=10
# log_duration=200
log_interval=50
output_folder=/home/dhupee/.mangologs

# ========== FORCE ALL LOGGING ==========
log_frametime=1
log_gpu_stats=1
log_cpu_stats=1
log_io=1
log_ram=1
log_swap=1

# ========== GPU FIX FOR AMD ==========
# Force GPU stats to work with RADV driver
gpu_stats=1
# Try this if GPU stats still show 0
# RADV_DEBUG=zerovram
